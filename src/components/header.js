const React = require('react');

export default class Header extends React.Component{
    render(){
        return(
            <div>
                <h1>头部在这里</h1>
            </div>
        )
    }
}
